import {
  formattedAddress
} from '../../../helpers/formatted-address';
import { module, test } from 'qunit';

module('FormattedAddressHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formattedAddress(42);
  assert.ok(result);
});
