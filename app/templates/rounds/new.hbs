{{nearest-courses store=store nearestCourses=nearestCourses}}

{{#if nearestCourses}}
  You lookin' for one-a these?
  <ul>
    {{#each nearestCourse in nearestCourses}}
      <li class='u-pointer'><a {{action 'updateCourseInput' nearestCourse}}>{{nearestCourse.name}}</a></li>
    {{/each}}
  </ul>
{{/if}}

<h2 class="text-center">
  Start a New Round
</h2>
<form {{action "createRound" on="submit"}}>
  <fieldset>
    <div class="form-group">
      <label for="course">
        * Course
      </label>
      {{view "select" content=courses selection=course.content optionValuePath="content.id" optionLabelPath="content.name" prompt="Select Course" id="course" required=true classNames="form-control"}}
    </div>
    <p>
      Can't see the course you're looking for? Why not
      {{link-to "suggest a new course?" "courses.new"}}
    </p>
    <div class="form-group">
      <label for="players">
        Players
      </label>
      <div class="row">
        <div class="col-xs-9 form-group">
          {{auto-complete id="players" model=potentialPlayers selection=selectedPlayer value=query displayKey="fullName"}}
        </div>
        <div class="col-xs-3">
          <button {{action 'addPlayer'}} {{bind-attr disabled="noPlayerSelected"}} class="btn btn-block btn-lg btn-success">
            <i class="fa fa-plus"></i>
          </button>
        </div>
      </div>
    </div>
    <div class="row block-grid-xs-3 block-grid-sm-4 block-grid-lg-6">
      <div class="block-grid-item">
        {{#if currentUser.hasAvatar}}
          <img {{bind-attr src="currentUser.avatarUrl"}} class="u-circle friend-avatar" />
        {{else}}
          <img src="http://xcellis.com/wp-content/uploads/2013/09/avatar-placeholder.png" class="u-circle friend-avatar" />
        {{/if}}
        <div class="text-center">
          {{currentUser.fullName}}
        </div>
      </div>
      {{#each player in players}}
        <div class="block-grid-item">
          {{#if player.hasAvatar}}
            <img {{bind-attr src="player.avatarUrl"}} class="u-circle friend-avatar" />
          {{else}}
            <img src="http://xcellis.com/wp-content/uploads/2013/09/avatar-placeholder.png" class="u-circle friend-avatar" />
          {{/if}}
          <div class="text-center">
            {{player.fullName}}
          </div>
          <button {{action 'removePlayer' player}} class="btn btn-block btn-danger">
            <i class="fa fa-minus"></i>
          </button>
        </div>
      {{/each}}
    </div>
    <button type="submit" class="btn btn-block btn-lg btn-primary">
      Start Round
    </button>
  </fieldset>
</form>
