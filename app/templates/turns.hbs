<div class="row">
  <div class="pull-left">
    <img src="http://res.cloudinary.com/frolfr/image/upload/v1429117674/basket_t3cpku.png" alt="Hole #" width="50" height="50" class="pull-left" />
    <h2 class="text-center pull-left hole-number">
      {{holeNumber}}
    </h2>
  </div>
  <div class="pull-right">
    <div class="btn-group pars">
      <button {{bind-attr class=":btn :btn-default :btn-lg isPar3:darkened"}} {{action "changePar" 3 on="click"}}>
        3
      </button>
      <button {{bind-attr class=":btn :btn-default :btn-lg isPar4:darkened"}} {{action "changePar" 4 on="click"}}>
        4
      </button>
      <button {{bind-attr class=":btn :btn-default :btn-lg isPar5:darkened"}} {{action "changePar" 5 on="click"}}>
        5
      </button>
    </div>
  </div>
</div>
{{#each turn in controller}}
  <div class="media turn-row">
    <div class="media-left media-middle">
      {{#if turn.scorecard.hasUserAvatar}}
        <img {{bind-attr src="turn.scorecard.userAvatarUrl"}} class="media-object turns-thumbnail thumbnail u-circle" />
      {{else}}
        <h2 class="media-object">
          {{turn.scorecard.userInitials}}
        </h2>
      {{/if}}
    </div>
    <div class="media-body row">
      <div class="vertical-align">
        <button {{action "subtractOne" turn on="click"}} class="btn btn-primary col-xs-3 text-center u-condensed btn-lg">
          <i class="fa fa-minus"></i>
        </button>
        <div class="col-xs-3">
          <h3 {{bind-attr class=":text-center turn.parStatus :par-status"}}>
            {{turn.displayStrokes}}
          </h3>
        </div>
        <button {{action "addOne" turn on="click"}} class="btn btn-primary col-xs-3 text-center u-condensed btn-lg">
          <i class="fa fa-plus"></i>
        </button>
        <div class="col-xs-3 text-center">
          <div class="pull-right">
            <div class="pull-left total">
              {{turn.scorecard.formattedTotals}}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
{{/each}}
<div class="push-down"></div>

<div class="well row block-grid-xs-3">
  <div class="block-grid-item">
    {{#unless isFirstHole}}
      <a {{action "previousHole" on="click"}} class="btn btn-success btn-block">
        <i class="fa fa-chevron-left"></i>
      </a>
    {{/unless}}
  </div>

  <div class="block-grid-item">
    {{#link-to 'round.settings' roundId classNames='btn btn-default btn-block'}}
      <i class="fa fa-gear"></i>
      Settings
    {{/link-to}}
  </div>

  <div class="block-grid-item">
    <a {{action "nextHole" on="click"}} class="btn btn-success btn-block">
      {{#if isLastHole}}
        <span>
          Recap
        </span>
      {{else}}
        <i class="fa fa-chevron-right"></i>
      {{/if}}
    </a>
  </div>
</div>
